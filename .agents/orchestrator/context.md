# HeelsUp Redesign Context

## Codebase Summary
- Roots: `frontend/` (contains React/Vite codebase), `src/` (worker/backend logic), `wrangler.toml` (Cloudflare Worker settings)
- Database schema: `schema/` (SQL files or schema definitions)
- Settings: Settings forms/controls alignment with backend configuration APIs
- Database Console: `DbConsole.tsx` (to be removed completely)

## Current Status
- Project is initialized.
- Need to explore the codebase to map exact file paths, structures, and APIs.
